# Development Environment Variables
NODE_ENV=development
PORT=3001

# Database (Local PostgreSQL)
DATABASE_URL="sqlserver://localhost\SQLEXPRESS;database=InventoryDB;integratedSecurity=true;trustServerCertificate=true"

# JWT (Development secret - not for production)
JWT_SECRET=inventory_app_development_jwt_secret_key_2025
JWT_EXPIRES_IN=7d

# CORS (Development frontend URL)
FRONTEND_URL=http://localhost:5173

# Debug options
DEBUG=true
